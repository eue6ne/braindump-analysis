# 브레인 덤프 분석 프로젝트 — 노션 데이터베이스 가이드라인

---

## 1. 예약 컬럼 (이름 변경 불가)

아래 4개 컬럼은 코드에서 고정으로 사용하므로 **반드시 이 이름 그대로** 사용해야 합니다.

| 컬럼명 | 노션 타입 | 설명 |
|--------|-----------|------|
| `날짜` | Date | 시계열 인덱스, 하루 1개 기록 |
| `감정지수` | Number | 종속변수, 1~10 숫자로 입력 |
| `브레인 덤프` | Text | 자유 기술 일기, 텍스트 마이닝 대상 |
| `오늘의 요약` | Title | 노션 PK 컬럼, 하루를 한 줄로 요약 |

> `오늘의 요약`이 비어있는 행은 전처리 시 자동으로 제거됩니다.

---

## 2. 독립변수 네이밍 규칙

**`범주_변수명`** 형식을 사용합니다.

```
신체_수면시간 / 신체_수면의질 / 신체_카페인
환경_날씨 / 환경_외출여부
성과_몰입시간 / 성과_할일달성도
여가_취미만족도 / 여가_취미유형
```

- 언더스코어(`_`) 앞이 **범주**, 뒤가 **변수명**
- 같은 범주의 변수는 동일한 접두어 사용
- 범주와 변수명은 자유롭게 추가 가능

---

## 3. 노션 컬럼 타입별 입력 규칙

### Number (숫자형)
서열 의미가 있는 항목은 **반드시 숫자로 입력**합니다. 문자열·이모지 사용 시 분석에서 제외됩니다.

```
✅ 올바른 예시
신체_수면의질    : 1(매우 나쁨) ~ 10(매우 좋음)
신체_활동량      : 1(매우 낮음) ~ 5(매우 높음)
성과_할일달성도  : 1(매우 낮음) ~ 5(매우 높음)

❌ 잘못된 예시
신체_활동량      : "매우 낮음", "보통", "높음"
성과_할일달성도  : "☆☆★★★"
```

> 척도 기준은 노션 컬럼 설명란에 메모해두는 것을 권장합니다.

### Select (단일 선택형)
범주형 변수에 사용합니다. (예: 날씨, 기분 유형 등)

### Multi-select (다중 선택형)
- **반드시 `없음` 항목을 포함**해야 합니다.
- 해당 사항이 없는 날은 `없음`을 선택합니다.
- 아무 항목도 선택하지 않으면 결측치로 처리됩니다.

```
✅ 올바른 예시
여가_취미유형 선택지: 없음 / 게임 / 독서 / 운동 / 애니메이션 감상 / ...
```

### Checkbox (체크박스형)
코드에서 자동으로 `0/1`로 변환됩니다. 별도 처리 불필요합니다.

### Date (날짜형)
`날짜` 예약 컬럼에만 사용합니다.

---

## 4. 스크립트 실행 순서

```bash
# 1단계: 노션에서 데이터 수집
python notion_loader.py

# 2단계: 전처리 (분석 목적에 따라 옵션 선택)
python data_preprocessing.py --mode impute --outlier detect --scaler none
# 샘플 데이터 사용 시
python data_preprocessing.py --mode impute --outlier detect --scaler none --input sample_data.csv

# 3단계: EDA 시각화
python data_visualization.py
# 샘플 데이터 사용 시
python data_visualization.py --input sample_data_cleaned_impute_none.csv

# 4단계: 분석 (각 분석별 입력 파일 지정)
python analysis_factor.py
python analysis_regression.py
python analysis_clustering.py
python analysis_timeseries.py
python analysis_text.py
# 샘플 데이터 사용 시 --input sample_data_cleaned_impute_none.csv 추가
# 단, 회귀분석은 standard 스케일링 적용 파일 사용 권장
# python analysis_regression.py --input sample_data_cleaned_impute_standard.csv
```

### `--mode` 옵션
| 옵션 | 설명 | 권장 상황 |
|------|------|-----------|
| `impute` | 결측치 자동 대체 (범주형: 최빈값 / 수치형: KNN) | 데이터가 적을 때 (기본값) |
| `drop` | 결측치 있는 행 제거 | 데이터가 충분할 때 (90일+) |

### `--outlier` 옵션
| 옵션 | 설명 |
|------|------|
| `none` | 이상치 처리 생략 (기본값) |
| `detect` | IQR 방식으로 탐지 후 대화형으로 처리 방식 선택 |

`detect` 선택 시 흐름:
1. 탐지된 이상치 목록 출력
2. 처리 여부 확인 `[y/n]`
3. 처리 방식 선택: `[1] flag` (값 보존 + 플래그 컬럼 추가) / `[2] cap` (IQR 경계값으로 대체)
4. 컬럼별로 처리할 행 번호 입력: `1` / `1,3` / `all` / `skip`

### `--scaler` 옵션
| 옵션 | 설명 | 사용 분석 |
|------|------|-----------|
| `none` | 스케일링 생략 (기본값) | EDA, 상관계수, 시계열 |
| `standard` | 평균 0, 표준편차 1 변환 | 회귀분석, 요인분석, PCA |
| `minmax` | 0~1 범위 변환 | 클러스터링, KNN |

> 종속변수(`감정지수`)는 스케일링에서 자동 제외됩니다.

---

## 5. 분석별 전처리 권장 옵션

| 분석 | mode | scaler | 비고 |
|------|------|--------|------|
| EDA / 시각화 | impute | none | |
| 상관계수 | impute | none | |
| 요인분석 / PCA | impute | standard | 범주형 컬럼은 분석 파일에서 별도 처리 |
| 회귀분석 | impute | standard | 범주형 컬럼은 분석 파일에서 원핫 인코딩 |
| 클러스터링 | impute | minmax | |
| 텍스트 마이닝 | — | — | `브레인 덤프` 컬럼 별도 처리 |
| 시계열 분석 | impute | none | |

---

## 6. 데이터 권장 수량

| 분석 | 최소 | 권장 |
|------|------|------|
| EDA / 시각화 | 30일 | 60일 |
| 요인분석 / 회귀분석 | 50일 | 100일 |
| 클러스터링 | 60일 | 90일 |
| 텍스트 마이닝 | 60일 | 120일+ |
| 시계열 분석 | 90일 | 180일 |

> 권장 분석 시작 시점: **90일(3개월)** 이상 축적 후

---

## 7. Linux 한글 폰트 설정 (최초 1회)

```bash
sudo apt install fonts-nanum
rm -rf ~/.cache/matplotlib
```
---

## 8. EDA 시각화 해석 기준

### 상관관계 히트맵 (`eda_heatmap.png`)
수치형 변수들 간의 선형 관계 강도를 -1 ~ 1 사이의 값으로 표현합니다.

| 범위 | 해석 |
|------|------|
| 0.7 ~ 1.0 | 강한 양의 상관관계 (함께 증가) |
| 0.4 ~ 0.7 | 중간 양의 상관관계 |
| -0.4 ~ 0.4 | 약하거나 거의 없음 |
| -0.7 ~ -0.4 | 중간 음의 상관관계 |
| -1.0 ~ -0.7 | 강한 음의 상관관계 (반대로 움직임) |

> `감정지수`와 상관계수가 높은 변수일수록 회귀분석에서 유의미한 독립변수가 될 가능성이 높습니다.
> 독립변수끼리 상관계수가 0.8 이상이면 다중공선성 문제가 발생할 수 있으므로 회귀분석 시 주의가 필요합니다.

---

### 분포 히스토그램 (`eda_histograms.png`)
각 수치형 변수의 값 분포와 KDE(커널 밀도 추정) 곡선을 함께 표시합니다.

- **좌우 대칭(정규분포)**: 평균 근처에 데이터가 집중, 분석에 가장 적합한 형태
- **오른쪽으로 치우침(우편포)**: 낮은 값에 데이터가 몰림, 로그 변환을 고려할 수 있음
- **왼쪽으로 치우침(좌편포)**: 높은 값에 데이터가 몰림
- **봉우리가 두 개(이봉분포)**: 두 가지 패턴이 섞여 있을 가능성, 클러스터링에서 자연스럽게 분리될 수 있음
- **막대가 한쪽에 몰림**: 데이터 범위가 너무 좁거나 이상치가 있을 수 있음

---

### 범주형 변수별 박스플롯 (`eda_boxplots.png`)
범주형 변수(날씨 등)의 항목별로 `감정지수` 분포를 비교합니다.

- **박스**: 전체 데이터의 중간 50% 범위 (IQR)
- **박스 안 가로선**: 중앙값 (median)
- **수염(박스 바깥 선)**: 정상 범위 내 최솟값 ~ 최댓값
- **점(검은 점)**: 개별 데이터 포인트
- **박스 위치 차이가 클수록**: 해당 범주가 감정지수에 미치는 영향이 클 가능성
- **박스 크기가 클수록**: 해당 항목에서 감정지수의 편차가 큼

---

### 감정지수 추세선 (`eda_target_trend.png`)
시간 흐름에 따른 `감정지수` 변화와 선형 회귀 추세선을 함께 표시합니다.

- **빨간 추세선**: 기울기가 양수 → 시간이 지날수록 감정지수가 전반적으로 상승
- **파란 추세선**: 기울기가 음수 → 시간이 지날수록 감정지수가 전반적으로 하락
- **기울기 절댓값이 클수록**: 변화 속도가 빠름
- **실제값이 추세선에서 많이 벗어날수록**: 감정 기복이 심한 구간

> 추세선은 단순 선형 회귀이므로 장기 예측보다는 전반적인 경향 파악 용도로 활용하세요.

---

### 다중선택 항목별 막대그래프 (`eda_multiselect_*.png`)
다중선택 컬럼의 각 항목을 선택한 날의 `감정지수` 평균을 막대로 표시합니다.
빨간 점선은 전체 기간의 감정지수 평균입니다.

- **막대가 점선보다 높을수록**: 해당 활동을 한 날 감정지수가 평균보다 높은 경향
- **막대가 점선보다 낮을수록**: 해당 활동을 한 날 감정지수가 평균보다 낮은 경향
- **항목 간 차이가 클수록**: 활동 종류가 감정에 미치는 영향이 클 가능성

> 단순 평균 비교이므로 인과관계가 아닌 경향성으로만 해석하세요.
> 선택 횟수가 적은 항목(5회 미만)은 신뢰도가 낮을 수 있습니다.

---

### 수치형 변수 페어플롯 (`eda_pairplot.png`)
수치형 변수들 간의 산점도 행렬로, 히트맵보다 상세한 관계를 파악할 수 있습니다.

- **대각선**: 각 변수의 KDE 분포 곡선
- **산점도에서 점들이 우상향**: 양의 상관관계
- **산점도에서 점들이 우하향**: 음의 상관관계
- **점들이 퍼져있을수록**: 상관관계가 약함
- **직선에서 크게 벗어난 점**: 이상치 가능성

---

### 요일별 박스플롯 (`eda_weekday.png`)
요일별 `감정지수` 분포를 비교합니다. 데이터가 충분히 쌓인 후(90일+) 해석 신뢰도가 높아집니다.

- **주말(토·일)과 평일의 박스 위치 차이**: 주간 루틴이 감정에 미치는 영향
- **특정 요일의 박스가 높을수록**: 해당 요일에 전반적으로 기분이 좋은 경향
- **수염이 길수록**: 해당 요일의 감정 편차가 큼
- **데이터가 적은 요일(점이 1~2개)**: 아직 해석보다는 참고 수준으로만 활용

---

## 9. 샘플 데이터 안내 (sample_data.csv)

### 시나리오
입사 3개월차 직장인 적응기 (90일, 2027-04-05 ~ 2027-07-03)

### 구간별 패턴
| 구간 | 기간 | 특징 |
|------|------|------|
| 초반 긴장기 | 1~2주 | 낯선 환경, 감정 불안정 |
| 적응 위기 | 3~5주 | 실수, 눈치, 감정지수 하락 |
| 안정기 | 6~9주 | 업무 적응, 감정지수 상승 |
| 번아웃 | 10~12주 | 야근 증가, 무기력, 급격한 하락 |
| 회복기 | 13주~ | 서서히 회복 |

### 설계된 주요 패턴
- 수면시간 ↓ → 감정지수 ↓
- 업무강도 ↑ → 수면의질 ↓ → 감정지수 ↓
- 야근 多 → 번아웃 클러스터
- 월요일 감정 최저, 토요일 최고 (주간 주기성)

> 샘플 데이터는 위 시나리오를 기반으로 Claude의 도움을 받아 생성했습니다.
---

## 10. 요인분석 (PCA) 해석 기준

### 실행 방법
```bash
# 기본 (실제 데이터)
python analysis_factor.py

# 샘플 데이터
python analysis_factor.py --input sample_data_cleaned_impute_none.csv
```

> 요인분석은 스케일링이 내부적으로 적용되므로 `none` 스케일링 파일을 사용합니다.

### 생성 파일
| 파일 | 설명 |
|------|------|
| `outputs/factor_scree.png` | 주성분 수 선택 기준 |
| `outputs/factor_loadings.png` | 변수별 주성분 기여도 |
| `outputs/factor_biplot.png` | PC1 vs PC2 산점도 |

### 스크리 플롯 (`factor_scree.png`)
몇 개의 주성분을 사용할지 결정하는 그래프입니다.

- **Kaiser 기준 (고유값 ≥ 1)**: 고유값이 1 이상인 주성분만 선택
- **누적 분산 80% 기준**: 누적 설명량이 80%를 넘는 시점의 주성분 수 선택
- 두 기준이 다를 경우 더 적은 수를 선택하는 것을 권장
- 데이터가 적을수록 주성분 수가 많아지는 경향 → 90일+ 데이터에서 재확인 필요

### 적재량 히트맵 (`factor_loadings.png`)
각 변수가 주성분에 얼마나 기여하는지 보여줍니다.

| 범위 | 해석 |
|------|------|
| 0.4 이상 (빨강) | 해당 주성분과 강한 양의 연관 |
| -0.4 이하 (파랑) | 해당 주성분과 강한 음의 연관 |
| -0.4 ~ 0.4 | 연관 약함 |

- 같은 주성분에 높은 적재량을 가진 변수들은 함께 움직이는 경향
- 주성분 이름은 직접 해석 필요 (예: PC1에 업무강도·카페인·야근 높으면 "업무 스트레스 요인")

### 바이플롯 (`factor_biplot.png`)
- **점**: 각 날짜의 주성분 공간 위치
- **화살표 방향이 같을수록**: 두 변수 간 양의 상관관계
- **화살표 방향이 반대일수록**: 두 변수 간 음의 상관관계
- **화살표 길이가 길수록**: 해당 주성분에 대한 기여도가 큼

---

## 11. 회귀분석 해석 기준

### 실행 방법
```bash
# 기본 (실제 데이터, standard 스케일링 권장)
python analysis_regression.py

# 샘플 데이터
python analysis_regression.py --input sample_data_cleaned_impute_standard.csv
```

> 회귀분석은 변수 간 스케일 차이가 결과에 영향을 주므로 `standard` 스케일링 파일을 사용합니다.

### 생성 파일
| 파일 | 설명 |
|------|------|
| `outputs/regression_coefficients.png` | 변수별 회귀계수 |
| `outputs/regression_residuals.png` | 잔차 진단 4종 플롯 |
| `outputs/regression_actual_vs_predicted.png` | 실제값 vs 예측값 |
| `outputs/regression_regularized_comparison.png` | OLS / Ridge / Lasso 계수 비교 (VIF > 10 시 생성) |

### VIF (분산팽창지수)
다중공선성(독립변수끼리 지나치게 상관된 문제)을 확인하는 지표입니다.

| VIF | 해석 |
|-----|------|
| < 5 | 양호 |
| 5 ~ 10 | 주의 |
| > 10 | 다중공선성 문제 → Ridge/Lasso 자동 실행  |

> **주의**: `standard` 스케일링 파일을 사용해야 VIF가 정확하게 계산됩니다.
> `none` 스케일링 파일 사용 시 변수 간 스케일 차이로 VIF가 왜곡되어 실제로는 문제없는 변수가 VIF > 10으로 나올 수 있습니다.

### 다중공선성 문제 발생시, Lidge / Lasso 자동 실행
VIF > 10 변수가 감지되면 OLS 완료 후 자동으로 Ridge / Lasso도 실행됩니다.

- **Ridge**: 모든 변수를 유지하면서 계수를 축소, 다중공선성에 강함
- **Lasso**: 불필요한 변수의 계수를 0으로 만들어 자동 변수 선택
- 최적 정규화 강도(alpha)는 Cross-Validation으로 자동 탐색
- VIF 양호 시 OLS만 실행, VIF > 10 감지 시 세 모델 R² 비교 출력

### Ridge / Lasso 비교 시각화 (`regression_regularized_comparison.png`)
VIF > 10 변수가 있을 때만 생성됩니다.

- OLS / Ridge / Lasso 세 모델의 회귀계수를 나란히 비교
- Lasso에서 0이 된 변수는 모델이 불필요하다고 판단한 것
- Ridge와 OLS 계수 차이가 클수록 다중공선성 영향이 컸던 변수

### 회귀계수 (`regression_coefficients.png`)
- **빨강 막대 (양수)**: 해당 변수 증가 시 감정지수 상승
- **파랑 막대 (음수)**: 해당 변수 증가 시 감정지수 하락
- **막대 길이가 길수록**: 감정지수에 미치는 영향이 큼
- **`*` 표시**: p-value < 0.05로 통계적으로 유의미한 변수

### 핵심 지표
| 지표 | 해석 |
|------|------|
| R² | 모델이 감정지수 분산을 설명하는 비율 (1에 가까울수록 좋음) |
| Adj. R² | 변수 수를 보정한 R² (변수가 많을수록 R²가 부풀려지므로 이 값으로 비교) |
| F-통계량 p값 | < 0.05이면 모델 전체가 통계적으로 유의미 |
| AIC | 낮을수록 좋은 모델 (변수 선택 시 비교 기준) |

### 잔차 진단 (`regression_residuals.png`)
| 플롯 | 이상적인 형태 |
|------|--------------|
| 잔차 vs 예측값 | 무작위로 퍼져있고 패턴 없음 |
| 잔차 분포 | 정규분포(종 모양)에 가까움 |
| Q-Q 플롯 | 점들이 대각선에 밀착 |
| 표준화 잔차 | 대부분 ±2 이내 |

> 데이터가 90일 미만일 경우 p-value 대부분이 유의미하지 않게 나올 수 있습니다.
> 이는 모델 문제가 아니라 표본 크기 부족으로 인한 통계적 검정력 한계입니다.